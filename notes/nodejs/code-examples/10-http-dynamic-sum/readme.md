## Dynamic Sum

### Create a Node.js application that:
- Displays a form at the root URL (/) where the user can enter two numbers.
- Accepts the numbers via a GET request to /process.
- Responds with the sum is #result (example: enters 5 and 7 → The sum is 12).

